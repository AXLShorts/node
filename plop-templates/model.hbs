import mongoose from 'mongoose';

import { I{{pascalCase name}} } from '../interface/index.js';

const {{camelCase name}}Schema = new mongoose.Schema<I{{pascalCase name}}>(
  {
    // Schema fields will be defined here
  },
  {
    timestamps: true,
  }
);

export const {{pascalCase name}}Model = mongoose.model<I{{pascalCase name}}>('{{pascalCase name}}', {{camelCase name}}Schema);
