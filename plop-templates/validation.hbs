import { z } from 'zod';

export const create{{pascalCase name}}Schema = z.object({
{{#each schemaFields}}
  {{this.name}}: {{this.zodType}},
{{/each}}
});

export const update{{pascalCase name}}Schema = z.object({
{{#each schemaFields}}
  {{this.name}}: {{this.zodType}}.optional(),
{{/each}}
});

export const {{camelCase name}}QuerySchema = z.object({
  id: z.string().min(1, 'ID is required'),
});

export type Create{{pascalCase name}}Request = z.infer<typeof create{{pascalCase name}}Schema>;
export type Update{{pascalCase name}}Request = z.infer<typeof update{{pascalCase name}}Schema>;
export type {{pascalCase name}}QueryRequest = z.infer<typeof {{camelCase name}}QuerySchema>;
